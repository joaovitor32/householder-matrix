import { IResponse } from './src/libs/qr_decomposition';

export function householder_matrix(index: number, x: number[]): number[][];

export default function qr_decomposition(matrix: number[][]): IResponse;
